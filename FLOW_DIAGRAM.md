# 🔄 Diagrama de Fluxo - Isolamento de Validação

## Estado do Componente

```
TravelRequestForm
├── type: "flight" | "hotel" | "car"
├── selectedOptionId: string
├── outboundOption: TravelOption | null
├── returnOption: TravelOption | null
│
├── options: TravelOption[]
├── searchStatistics: {bestCombinedPrice: number}
│
├── NOVO: currentSearchLowestPrice: number | null  ← ISOLAMENTO
│
└── cartItems: CartItem[]
```

## Fluxo Completo

```
┌─────────────────────────────────────────────────────────────┐
│ 1. SELECIONAR TIPO (Voo/Hotel/Carro)                        │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ handleTypeChange(newType)                                   │
├─ setType(newType)                                           │
├─ setOptions([])          ← Limpar opções anteriores         │
├─ setSearchStatistics(null)                                  │
├─ setCurrentSearchLowestPrice(null) ← ISOLAMENTO             │
├─ setOutboundOption(null)                                    │
├─ setReturnOption(null)                                      │
└─ Todos os estados de input resetados                        │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. PREENCHER DADOS E PESQUISAR                              │
│    (origem, destino, datas, etc)                            │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ handleSearch()                                              │
├─ Validar campos obrigatórios                               │
├─ setIsSearching(true)                                       │
├─ Chamar searchOptionsAction()                              │
└─ Aguardar resultados                                       │
                         ↓
                    (Resultados)
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ setOptions(resultOptions)                                   │
│ setSearchStatistics(resultStats)                            │
│                                                              │
│ ✨ ISOLAMENTO AQUI:                                          │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ IF round-trip flight:                                  │ │
│ │   setCurrentSearchLowestPrice(bestCombinedPrice) ✨     │ │
│ │ ELSE:                                                  │ │
│ │   setCurrentSearchLowestPrice(findLowestPrice(...))✨   │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. RENDERIZAR RESULTADOS E SELECIONAR OPÇÃO                 │
│                                                              │
│ Para cada opção:                                            │
│   {currentSearchLowestPrice !== null &&                     │
│    option.price > currentSearchLowestPrice && (             │
│      <AlertCircle> "Opção acima do menor preço"            │
│    )}                                                       │
│                                                              │
│ Button "Adicionar ao Carrinho" desabilitado se:             │
│   !reason ||                                                │
│   (currentSearchLowestPrice !== null &&                     │
│    selectedPrice > currentSearchLowestPrice &&              │
│    !justification)                                          │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ handleSelectOption(optionId) [para round-trip]             │
├─ Se é voo de ida:  setOutboundOption(...)                  │
├─ Se é voo de volta: setReturnOption(...)                   │
└─ Exibir card de confirmação                                │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. PREENCHER INFORMAÇÕES DO ITEM                            │
│    (motivo, justificativa se necessário)                    │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ handleAddToCart()                                           │
│                                                              │
│ Calcular itemPrice:                                         │
│   IF round-trip:                                            │
│     itemPrice = outboundPrice + returnPrice                │
│   ELSE:                                                     │
│     itemPrice = selectedOption.price                       │
│                                                              │
│ ✨ VALIDAÇÃO ISOLADA AQUI:                                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ itemNeedsJustification =                                │ │
│ │   currentSearchLowestPrice !== null &&                  │ │
│ │   itemPrice > currentSearchLowestPrice                  │ │
│ │                                                         │ │
│ │ IF itemNeedsJustification && !justification:            │ │
│ │   toast.error("Justificativa obrigatória")             │ │
│ │   RETURN                                                │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                              │
│ Criar newItem:                                              │
│   justification: itemNeedsJustification ? justification : null
│   selectedOption: {...}                                     │
│                                                              │
│ ✨ LIMPEZA AQUI:                                             │
│ setCurrentSearchLowestPrice(null) ← Pronto para próxima     │
│ setOptions([])                                              │
│ setSearchStatistics(null)                                   │
│ setJustification("")                                        │
│ ... outros resets                                           │
│                                                              │
│ setCartItems([...cartItems, newItem])                       │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. EXIBIR NO CARRINHO                                       │
│                                                              │
│ Para cada cartItem:                                         │
│   IF item.needsJustification:  ← De CartItem, não global    │
│     <Aviso com item.justification>                          │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. CHECKOUT (Finalizar Compra)                              │
│                                                              │
│ Exibir resumo:                                              │
│   Para cada cartItem:                                       │
│     Detalhes do item                                        │
│     IF item.needsJustification:                             │
│       <Aviso individual>                                    │
│     Total                                                   │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ handleSubmitCart()                                          │
├─ Validar carrinho não vazio                                │
├─ Chamar submitCartAction(cartItems)                        │
└─ Exibir sucesso/erro                                       │
└─────────────────────────────────────────────────────────────┘
```

## Estados Críticos

### ✅ Estado Correto (Após Búsca)
```javascript
{
  type: "flight",
  options: [/* 30 voos */],
  searchStatistics: { bestCombinedPrice: 845 },
  currentSearchLowestPrice: 845,  ← DEFINIDO CORRETAMENTE
  selectedOptionId: "",
  outboundOption: null,
  returnOption: null
}
```

### ✅ Estado Correto (Após Mudar Tipo)
```javascript
{
  type: "hotel",
  options: [],
  searchStatistics: null,
  currentSearchLowestPrice: null,  ← RESETADO CORRETAMENTE
  selectedOptionId: "",
  outboundOption: null,
  returnOption: null
}
```

### ✅ Estado Correto (Nova Busca)
```javascript
{
  type: "hotel",
  options: [/* 10 hotéis */],
  searchStatistics: null,
  currentSearchLowestPrice: 200,   ← NOVO CONTEXTO
  selectedOptionId: "hotel-1"
}
```

## Pontos de Isolamento

```
┌─────────────────────────────────────────────────────┐
│ PONTO 1: handleSearch()                             │
│ ─────────────────────────────────────────────────── │
│ setCurrentSearchLowestPrice(newValue)               │
│ → Define para a busca ATUAL                        │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ PONTO 2: handleTypeChange()                         │
│ ─────────────────────────────────────────────────── │
│ setCurrentSearchLowestPrice(null)                   │
│ → Limpa antes de nova categoria                    │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ PONTO 3: handleAddToCart()                          │
│ ─────────────────────────────────────────────────── │
│ itemNeedsJustification =                            │
│   currentSearchLowestPrice !== null && ...          │
│ → Valida usando APENAS dados locais                │
│                                                     │
│ setCurrentSearchLowestPrice(null)                   │
│ → Reseta para próxima busca                        │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ PONTO 4: Renderização                              │
│ ─────────────────────────────────────────────────── │
│ {currentSearchLowestPrice !== null && ... && (      │
│   <Aviso>                                           │
│ )}                                                  │
│ → Aviso baseado em dados isolados                  │
└─────────────────────────────────────────────────────┘
```

## Comparação: Antes vs Depois

```
ANTES (Global Reativo):
┌─────────┐     ┌──────┐     ┌──────┐
│  Voo    │────→│Hotel │────→│Carro │
│ needsJ= │     │needsJ=│     │needsJ=│
│  TRUE   │  X  │ TRUE │  X  │ TRUE │  ← WRONG!
└─────────┘     └──────┘     └──────┘

DEPOIS (Local Isolado):
┌─────────────────┐
│  Voo - Busca 1  │
│ currentLowest=845│
│ selection=845    │
│ needsJ=FALSE ✅  │
└─────────────────┘
         ↓ [handleTypeChange]
         ↓ [currentLowestPrice=null]
┌─────────────────┐
│Hotel - Busca 2  │
│currentLowest=200│
│ selection=200    │
│ needsJ=FALSE ✅  │
└─────────────────┘
         ↓ [handleTypeChange]
         ↓ [currentLowestPrice=null]
┌─────────────────┐
│Carro - Busca 3  │
│currentLowest=150│
│ selection=250    │
│ needsJ=TRUE  ✅  │
└─────────────────┘
```
